<h1>製品を登録するよ(⋈◍＞◡＜◍)。✧♡</h1>

<%= form_for @product,:url => {controller: "products", action: "create"} do |f| %>
    <p>
        <%= f.label :name, '製品名' %>
        <%= f.text_field :name %>
    </p>
    <p>
        <%= f.label :price, '値段' %>
        <%= f.number_field :price %>
    </p>
    <%= f.submit '新規登録'%>
<% end %>